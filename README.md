# Ftest
Fake Input Test Script Language Support Plugin

## Features
- Support for `.ftest` script files
- Real-time syntax validation (assignment `=` / assertions `==`, `<`, `>`)
- Syntax highlighting
- Flexible support for custom `stepN` steps